---
description: 完成修改后自动验证、记录修改内容并推送到 GitHub
alwaysApply: true
---

# GitHub 交付规则

- 完成用户要求的代码修改后，先运行与风险匹配的类型检查、测试和构建。
- 验证通过后，检查 `git diff`，只提交本次任务相关文件，不夹带无关改动。
- 提交标题使用清晰的中文 Conventional Commit，例如 `feat: 支持选中文本做笔记`。
- 提交正文写明主要修改内容和验证结果，避免使用“更新代码”等含糊描述。
- 自动推送当前分支到它已配置的 GitHub 上游，并在回复中提供提交哈希和推送结果。
- 如果验证失败、功能未完成、远程存在冲突、提交可能包含密钥或推送需要新授权，停止推送并向用户说明。
- 禁止强制推送、改写远程历史或绕过分支保护，除非用户针对该操作明确授权。
